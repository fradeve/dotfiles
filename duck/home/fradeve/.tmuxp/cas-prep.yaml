session_name: cas-prep
start_directory: /home/fradeve/git/cas/cas
windows:
    - window_name: runserver | celery
      focus: true
      layout: main-vertical
      panes:
          - shell_command:
              - ssh cas-prep-1
              - sudo su - devops
              - cd /opt/cas
              - source venv/bin/activate
              - ./manage.py shell
          - shell_command:
              - ssh cas-prep-1
              - tail -f /opt/var/log/cas/celery_w1.log
          - shell_command:
              - ssh cas-prep-2
              - tail -f /opt/var/log/cas/celery_w1.log
